--index-url https://pypi.org/simple
datasets==5.0.0
huggingface_hub==1.24.0
numpy==2.5.1
pyarrow==25.0.0
PyYAML==6.0.3
safetensors==0.8.0
tokenizers==0.23.1
tqdm==4.69.0
pytest==9.1.1

# Install the CUDA build separately on this machine:
# python -m pip install torch==2.13.0 --index-url https://download.pytorch.org/whl/cu130

